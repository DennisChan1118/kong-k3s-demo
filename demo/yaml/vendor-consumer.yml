apiVersion: configuration.konghq.com/v1
kind: KongConsumer
metadata:
  name: vendor-consumer
  annotations:
    kubernetes.io/ingress.class: kong
username: Vendor 1
custom_id: vendor-a
credentials:
- vendor-apikey