apiVersion: configuration.konghq.com/v1
kind: KongConsumer
metadata:
  name: vendor-consumer
  annotations:
    kubernetes.io/ingress.class: kong
username: Vendor A
custom_id: vendor-a
credentials:
- vendor-apikey